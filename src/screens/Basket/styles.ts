import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cardCover: {
    width: '80%',
    maxWidth: 400,
    backgroundColor: 'red',
    height: 100,
  },
  container: {flex: 1},
});

export default styles;
